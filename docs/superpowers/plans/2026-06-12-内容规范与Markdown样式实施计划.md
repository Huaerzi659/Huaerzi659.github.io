# 内容规范与 Markdown 样式 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 Waltz, Then Think 建立可长期维护的内容规范体系，包括写作规范、文章模板、自动内容检查和更完整的 Markdown 正文样式。

**Architecture:** 内容规则落在 `docs/内容规范.md` 和 `docs/templates/post-template.md`；可自动验证的规则落在 `scripts/verify-content.mjs` 与 `package.json`；页面阅读体验落在 `src/styles/global.css` 的 `.prose` 样式；样式覆盖情况通过 `scripts/verify-ux.mjs` 做静态 token 检查。

**Tech Stack:** Astro 6、Markdown content collections、Node.js ESM scripts、CSS custom properties，不新增运行时依赖。

---

## File Structure

- Create: `docs/内容规范.md`
  - 面向站主的中文写作规范，解释文件命名、frontmatter、标签、Markdown、图片、发布前检查。
- Create: `docs/templates/post-template.md`
  - 新文章模板，不放入 `src/content/posts/`，避免被 Astro 构建成正式文章。
- Create: `scripts/verify-content.mjs`
  - 使用 Node 原生模块检查 Markdown 内容规则。
- Modify: `package.json`
  - 新增 `verify:content` 脚本。
- Modify: `scripts/verify-ux.mjs`
  - 增加 `.prose` 对引用、图片、表格、分割线、主题化样式的 token 检查。
- Modify: `src/styles/global.css`
  - 替换并扩展 `.prose` 样式块，覆盖标题、段落、链接、列表、引用、图片、表格、代码块、分割线和移动端滚动。

---

### Task 1: Add Failing Content Verification Entry

**Files:**
- Modify: `package.json`

- [ ] **Step 1: Add the npm script before the file exists**

Add `verify:content` under the existing verification scripts:

```json
{
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "verify:content": "node scripts/verify-content.mjs",
    "verify:rss": "node scripts/verify-rss.mjs",
    "verify:motion": "node scripts/verify-motion.mjs",
    "verify:ux": "node scripts/verify-ux.mjs"
  }
}
```

- [ ] **Step 2: Run the new check and verify it fails**

Run:

```powershell
npm run verify:content
```

Expected: FAIL because `scripts/verify-content.mjs` does not exist yet.

- [ ] **Step 3: Commit the failing check boundary**

```powershell
git add package.json
git commit -m "test: add content verification entry"
```

---

### Task 2: Implement Content Verification Script

**Files:**
- Create: `scripts/verify-content.mjs`

- [ ] **Step 1: Create the verifier**

Create `scripts/verify-content.mjs` with this content:

```js
import { existsSync } from "node:fs";
import { readdir, readFile } from "node:fs/promises";
import path from "node:path";

const root = process.cwd();
const postsDir = path.join(root, "src", "content", "posts");
const imageRootPrefix = "/images/articles/";
const allowedTags = new Set(["技术", "经验", "感想", "灵感", "工具", "复盘"]);
const filenamePattern = /^[a-z0-9]+(?:-[a-z0-9]+)*\.md$/;
const datePattern = /^\d{4}-\d{2}-\d{2}$/;

const failures = [];

function addFailure(file, message) {
  failures.push(`${file}: ${message}`);
}

function parseFrontmatter(markdown) {
  const match = markdown.match(/^---\r?\n([\s\S]*?)\r?\n---\r?\n?/);

  if (!match) {
    return { data: null, body: markdown };
  }

  const raw = match[1];
  const body = markdown.slice(match[0].length);
  const data = {};

  for (const line of raw.split(/\r?\n/)) {
    const found = line.match(/^([A-Za-z0-9_-]+):\s*(.*)$/);

    if (found) {
      data[found[1]] = found[2].trim();
    }
  }

  return { data, body };
}

function unquote(value = "") {
  return value.replace(/^["']|["']$/g, "").trim();
}

function parseInlineArray(value = "") {
  const trimmed = value.trim();

  if (!trimmed.startsWith("[") || !trimmed.endsWith("]")) {
    return null;
  }

  const inside = trimmed.slice(1, -1).trim();

  if (!inside) {
    return [];
  }

  return inside
    .split(",")
    .map((item) => unquote(item.trim()))
    .filter(Boolean);
}

function checkFrontmatter(file, data) {
  if (!data) {
    addFailure(file, "missing frontmatter block");
    return;
  }

  for (const key of ["title", "description", "date", "tags", "featured"]) {
    if (!(key in data)) {
      addFailure(file, `missing frontmatter field: ${key}`);
    }
  }

  const title = unquote(data.title);
  const description = unquote(data.description);
  const date = unquote(data.date);
  const tags = parseInlineArray(data.tags);
  const featured = data.featured;

  if (title && (title.length < 2 || title.length > 40)) {
    addFailure(file, "title should be between 2 and 40 characters");
  }

  if (description && (description.length < 20 || description.length > 120)) {
    addFailure(file, "description should be between 20 and 120 characters");
  }

  if (date && !datePattern.test(date)) {
    addFailure(file, "date must use YYYY-MM-DD format");
  }

  if (featured && !["true", "false"].includes(featured)) {
    addFailure(file, "featured must be true or false");
  }

  if (!Array.isArray(tags)) {
    addFailure(file, 'tags must use inline array syntax, for example ["技术", "经验"]');
    return;
  }

  if (tags.length < 1 || tags.length > 3) {
    addFailure(file, "tags should contain 1 to 3 items");
  }

  for (const tag of tags) {
    if (!allowedTags.has(tag)) {
      addFailure(file, `unknown tag "${tag}". Allowed tags: ${Array.from(allowedTags).join(", ")}`);
    }
  }
}

function checkHeadings(file, body) {
  const headingLevels = [];

  for (const line of body.split(/\r?\n/)) {
    const heading = line.match(/^(#{1,6})\s+\S/);

    if (!heading) {
      continue;
    }

    const level = heading[1].length;

    if (level === 1) {
      addFailure(file, "body should not contain # headings; frontmatter title renders the page h1");
    }

    headingLevels.push(level);
  }

  for (let index = 1; index < headingLevels.length; index += 1) {
    const previous = headingLevels[index - 1];
    const current = headingLevels[index];

    if (current > previous + 1) {
      addFailure(file, `heading level jumps from h${previous} to h${current}`);
    }
  }
}

function checkImages(file, body) {
  const images = body.matchAll(/!\[([^\]]*)\]\(([^)]+)\)/g);

  for (const image of images) {
    const alt = image[1].trim();
    const src = image[2].trim();

    if (!alt) {
      addFailure(file, `image ${src} is missing alt text`);
    }

    if (src.startsWith("/")) {
      if (!src.startsWith(imageRootPrefix)) {
        addFailure(file, `local image ${src} should live under ${imageRootPrefix}`);
        continue;
      }

      const localPath = path.join(root, "public", src);

      if (!existsSync(localPath)) {
        addFailure(file, `local image does not exist: ${src}`);
      }

      const imageFile = path.basename(src);

      if (!/^[a-z0-9]+(?:-[a-z0-9]+)*\.(svg|png|jpg|jpeg|webp|gif)$/.test(imageFile)) {
        addFailure(file, `image filename should use lowercase kebab-case: ${imageFile}`);
      }
    }
  }
}

function checkCodeFences(file, body) {
  const fenceCount = (body.match(/^```/gm) ?? []).length;

  if (fenceCount % 2 !== 0) {
    addFailure(file, "code fences must be paired");
  }
}

function checkTables(file, body) {
  const lines = body.split(/\r?\n/);

  for (let index = 0; index < lines.length - 1; index += 1) {
    const current = lines[index].trim();
    const next = lines[index + 1].trim();

    if (current.startsWith("|") && current.endsWith("|")) {
      const looksLikeSeparator = /^\|[\s:-]+\|(?:[\s:-]+\|)*$/.test(next);

      if (!looksLikeSeparator) {
        addFailure(file, `table header on line ${index + 1} must be followed by a separator row`);
      }
    }
  }
}

const postFiles = (await readdir(postsDir)).filter((file) => file.endsWith(".md"));

if (postFiles.length === 0) {
  addFailure("src/content/posts", "no Markdown posts found");
}

for (const file of postFiles) {
  if (!filenamePattern.test(file)) {
    addFailure(file, "filename must use lowercase kebab-case");
  }

  const markdown = await readFile(path.join(postsDir, file), "utf8");
  const { data, body } = parseFrontmatter(markdown);

  checkFrontmatter(file, data);
  checkHeadings(file, body);
  checkImages(file, body);
  checkCodeFences(file, body);
  checkTables(file, body);
}

if (failures.length > 0) {
  console.error("Content verification failed:");

  for (const failure of failures) {
    console.error(`- ${failure}`);
  }

  process.exit(1);
}

console.log(`Content verification passed: ${postFiles.length} posts checked.`);
```

- [ ] **Step 2: Run the verifier**

Run:

```powershell
npm run verify:content
```

Expected: PASS with:

```text
Content verification passed: 4 posts checked.
```

If it fails, only make minimal corrections to existing Markdown that are directly required by the declared rules.

- [ ] **Step 3: Commit the verifier**

```powershell
git add scripts/verify-content.mjs
git commit -m "test: add markdown content verifier"
```

---

### Task 3: Add Content Guide And Post Template

**Files:**
- Create: `docs/内容规范.md`
- Create: `docs/templates/post-template.md`

- [ ] **Step 1: Create the guide document**

Create `docs/内容规范.md` with these sections:

```md
# Waltz, Then Think 内容规范

这份文档用来帮助你稳定地写文章、改文章和检查文章。它不是文学规则，而是一套维护网站时不会迷路的约定。

## 1. 写文章的固定流程

1. 先想清楚文章属于哪一类：技术教程、经验复盘、感想随笔。
2. 在 `src/content/posts/` 新建 Markdown 文件。
3. 文件名使用小写英文短横线，例如 `from-zero-to-blog.md`。
4. 从 `docs/templates/post-template.md` 复制 frontmatter。
5. 写完正文后运行 `npm run verify:content`。
6. 再运行常规验证和构建。

## 2. 文件命名

文章文件名会进入网址，所以发布后不要随便改。

正确：

```text
stable-internet-access.md
from-zero-to-blog.md
```

不推荐：

```text
我的文章.md
stable_internet_access.md
StableInternet.md
```

## 3. Frontmatter

每篇文章开头都要有：

```yaml
---
title: "文章标题"
description: "一句清楚的文章摘要"
date: "2026-06-12"
tags: ["技术"]
featured: false
---
```

`title` 是页面标题。正文里不要再写 `# 一级标题`。

`description` 是文章摘要，会用于文章卡片、搜索引擎和 RSS。它要说明读者能从文章里得到什么。

`date` 使用 `YYYY-MM-DD`。

`tags` 建议 1 到 3 个。

`featured` 控制是否出现在首页精选区域。

## 4. 标签池

当前推荐标签：

```text
技术
经验
感想
灵感
工具
复盘
```

新增标签前先想：以后会不会继续写这个主题。如果只会出现一次，优先用已有标签。

## 5. Markdown 标题

文章页面已经自动生成 `h1`，正文从 `##` 开始。

```md
## 第一节

### 第一节里的小节

#### 更细的小点
```

不要这样写：

```md
# 我的文章标题
```

## 6. 图片

图片放在：

```text
public/images/articles/<文章文件名去掉 .md>/
```

引用方式：

```md
![第一次配置流程图](/images/articles/stable-internet-access/setup-flow.svg)
```

图片 alt 文本要写清楚，不要空着。

## 7. 技术教程结构

```md
开头：说明适合谁、解决什么问题

## 先理解概念

## 准备工作

## 操作步骤

## 常见问题

## 总结

## 参考链接
```

## 8. 经验复盘结构

```md
开头：发生了什么

## 背景

## 我做了什么

## 哪里卡住了

## 解决方式

## 下次怎么做
```

## 9. 感想随笔结构

感想随笔可以松一点，但最好仍然有起承转合：

```text
开头：一个真实触发点
中段：展开想法
后段：回到自己的判断
```

## 10. 发布前检查

```powershell
npm run verify:content
npm run verify:ux
npm run verify:motion
npm run verify:rss
```

构建使用项目已知可用 Node：

```powershell
& 'C:\Users\刘\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe' .\node_modules\astro\bin\astro.mjs build
```

## 11. 常见错误

如果 `verify:content` 提示 `body should not contain # headings`，说明正文里用了一级标题。把它改成 `##`。

如果提示图片不存在，检查路径是不是从 `/images/articles/` 开始，以及图片是否真的在 `public/images/articles/` 里。

如果提示标签未知，先从标签池里选择一个已有标签。
```

- [ ] **Step 2: Create the article template**

Create `docs/templates/post-template.md` with this content:

```md
---
title: "文章标题"
description: "用一句话说明这篇文章解决什么问题，或者记录了什么想法。"
date: "2026-06-12"
tags: ["技术"]
featured: false
---

开头先告诉读者：这篇文章适合谁、解决什么问题、为什么值得读。

## 先理解概念

这里写背景、术语、前置知识。

## 操作或展开

这里写主要内容。

### 第 1 步：写清楚动作

每一步尽量说明目的和结果。

### 第 2 步：继续推进

不要只贴命令，也要解释为什么这么做。

## 常见问题

- 问题一：这里写现象和解决方式。
- 问题二：这里写现象和解决方式。

## 总结

用几句话收束：读者现在应该理解了什么，下一步可以做什么。

## 参考链接

- [链接标题](https://example.com/)
```

- [ ] **Step 3: Check the docs exist**

Run:

```powershell
Test-Path docs\内容规范.md
Test-Path docs\templates\post-template.md
```

Expected: both commands output `True`.

- [ ] **Step 4: Commit the docs**

```powershell
git add docs/内容规范.md docs/templates/post-template.md
git commit -m "docs: add blog content standards"
```

---

### Task 4: Add Failing UX Checks For Markdown Prose Styles

**Files:**
- Modify: `scripts/verify-ux.mjs`

- [ ] **Step 1: Extend the global.css token list**

In the `src/styles/global.css` check inside `scripts/verify-ux.mjs`, add these tokens:

```js
".prose h4",
".prose a",
".prose blockquote",
".prose img",
".prose table",
".prose th",
".prose td",
".prose hr",
"html[data-theme=\"anime-scroll\"] .prose blockquote",
"html[data-theme=\"anime-scroll\"] .prose img",
"html[data-theme=\"anime-scroll\"] .prose table",
"overflow-x: auto",
```

- [ ] **Step 2: Run the UX verifier and verify it fails**

Run:

```powershell
npm run verify:ux
```

Expected: FAIL because the current `.prose` CSS does not yet include these selectors.

- [ ] **Step 3: Commit the failing style expectation**

```powershell
git add scripts/verify-ux.mjs
git commit -m "test: require full markdown prose styling"
```

---

### Task 5: Implement Markdown Prose Styles

**Files:**
- Modify: `src/styles/global.css`

- [ ] **Step 1: Replace the existing `.prose` block**

Replace the current `.prose` styles from `.prose { ... }` through `.prose pre code { ... }` with:

```css
.prose {
  max-width: 740px;
  margin: 0 auto;
  color: #dbeafe;
  font-size: 1.06rem;
  line-height: 1.82;
}

html[data-theme="anime-scroll"] .prose {
  color: var(--copy);
}

.prose > :first-child {
  margin-top: 0;
}

.prose > :last-child {
  margin-bottom: 0;
}

.prose h2,
.prose h3,
.prose h4 {
  color: var(--ink);
  font-family: var(--font-display);
  line-height: 1.25;
}

.prose h2 {
  margin: 2.4em 0 0.8em;
  border-bottom: 1px solid rgba(0, 245, 255, 0.22);
  padding-bottom: 0.35em;
  font-size: clamp(1.55rem, 3vw, 2.1rem);
}

.prose h3 {
  margin: 2em 0 0.65em;
  font-size: clamp(1.25rem, 2.4vw, 1.55rem);
}

.prose h4 {
  margin: 1.7em 0 0.45em;
  color: var(--muted);
  font-size: 1.05rem;
}

html[data-theme="anime-scroll"] .prose h2 {
  border-bottom-color: rgba(26, 45, 76, 0.18);
}

.prose p {
  margin: 1.18em 0;
}

.prose a {
  color: var(--neon-cyan);
  text-decoration: underline;
  text-decoration-color: rgba(0, 245, 255, 0.42);
  text-decoration-thickness: 1px;
  text-underline-offset: 0.2em;
}

.prose a:hover {
  color: var(--neon-pink);
  text-decoration-color: rgba(255, 43, 214, 0.55);
}

html[data-theme="anime-scroll"] .prose a {
  color: #a13b12;
  text-decoration-color: rgba(249, 115, 22, 0.36);
}

html[data-theme="anime-scroll"] .prose a:hover {
  color: #0f5f86;
  text-decoration-color: rgba(15, 139, 179, 0.38);
}

.prose ul,
.prose ol {
  margin: 1.05em 0;
  padding-left: 1.35em;
}

.prose li + li {
  margin-top: 0.35em;
}

.prose blockquote {
  margin: 1.7em 0;
  border-left: 3px solid rgba(0, 245, 255, 0.5);
  background: rgba(0, 245, 255, 0.06);
  box-shadow: inset 16px 0 28px rgba(0, 245, 255, 0.04);
  padding: 0.9em 1.1em;
  color: #c7d2fe;
}

.prose blockquote p {
  margin: 0.65em 0;
}

html[data-theme="anime-scroll"] .prose blockquote {
  border-left-color: rgba(249, 115, 22, 0.72);
  background: rgba(249, 115, 22, 0.08);
  box-shadow: 4px 4px 0 rgba(26, 45, 76, 0.06);
  color: var(--copy);
}

.prose img {
  display: block;
  max-width: 100%;
  height: auto;
  margin: 2em auto;
  border: 1px solid rgba(0, 245, 255, 0.24);
  border-radius: var(--radius);
  background: rgba(2, 6, 23, 0.62);
  box-shadow: 0 18px 48px rgba(0, 0, 0, 0.26);
}

html[data-theme="anime-scroll"] .prose img {
  border-color: rgba(26, 45, 76, 0.18);
  background: rgba(255, 250, 240, 0.72);
  box-shadow: 6px 6px 0 rgba(26, 45, 76, 0.08);
}

.prose table {
  display: block;
  width: 100%;
  margin: 1.8em 0;
  overflow-x: auto;
  border-collapse: collapse;
  border: 1px solid rgba(0, 245, 255, 0.2);
  border-radius: var(--radius);
  background: rgba(2, 6, 23, 0.38);
}

.prose th,
.prose td {
  border-bottom: 1px solid rgba(0, 245, 255, 0.16);
  padding: 0.7em 0.9em;
  text-align: left;
  vertical-align: top;
  white-space: nowrap;
}

.prose th {
  color: var(--ink);
  font-family: var(--font-mono);
  font-size: 0.9em;
}

.prose tr:last-child td {
  border-bottom: 0;
}

html[data-theme="anime-scroll"] .prose table {
  border-color: rgba(26, 45, 76, 0.18);
  background: rgba(255, 250, 240, 0.68);
  box-shadow: 4px 4px 0 rgba(26, 45, 76, 0.05);
}

html[data-theme="anime-scroll"] .prose th,
html[data-theme="anime-scroll"] .prose td {
  border-bottom-color: rgba(26, 45, 76, 0.12);
}

.prose code {
  border: 1px solid rgba(0, 245, 255, 0.28);
  border-radius: 5px;
  background: rgba(0, 245, 255, 0.09);
  padding: 0.1em 0.35em;
  color: var(--neon-cyan);
  font-family: var(--font-mono);
  font-size: 0.92em;
}

html[data-theme="anime-scroll"] .prose code {
  border-color: rgba(249, 115, 22, 0.28);
  background: rgba(249, 115, 22, 0.1);
  color: #9a3412;
}

.prose pre {
  overflow-x: auto;
  border: 1px solid rgba(0, 245, 255, 0.32);
  border-radius: var(--radius);
  background: #020617;
  box-shadow: inset 0 0 24px rgba(0, 245, 255, 0.08);
  color: #ecfeff;
  padding: 18px;
}

html[data-theme="anime-scroll"] .prose pre {
  border-color: rgba(26, 45, 76, 0.28);
  background: #172033;
  box-shadow: inset 0 0 18px rgba(15, 139, 179, 0.06);
  color: #f8fafc;
}

.prose pre code {
  border: 0;
  background: transparent;
  color: inherit;
  padding: 0;
}

.prose hr {
  margin: 2.3em 0;
  border: 0;
  border-top: 1px solid rgba(0, 245, 255, 0.22);
}

html[data-theme="anime-scroll"] .prose hr {
  border-top-color: rgba(26, 45, 76, 0.18);
}
```

- [ ] **Step 2: Run UX verification**

Run:

```powershell
npm run verify:ux
```

Expected: PASS.

- [ ] **Step 3: Run content verification**

Run:

```powershell
npm run verify:content
```

Expected: PASS.

- [ ] **Step 4: Commit the prose styling**

```powershell
git add src/styles/global.css scripts/verify-ux.mjs
git commit -m "style: improve markdown prose rendering"
```

---

### Task 6: Run Full Verification And Build

**Files:**
- No source edits expected unless verification exposes a direct issue from this feature.

- [ ] **Step 1: Run content verification**

```powershell
npm run verify:content
```

Expected: PASS.

- [ ] **Step 2: Run UX verification**

```powershell
npm run verify:ux
```

Expected: PASS.

- [ ] **Step 3: Run motion verification**

```powershell
npm run verify:motion
```

Expected: PASS.

- [ ] **Step 4: Run RSS verification**

If `dist/rss.xml` already exists from a recent build, run:

```powershell
npm run verify:rss
```

Expected: PASS.

If it fails because `dist/rss.xml` is missing or stale, run the Astro build in Step 5 first, then run `npm run verify:rss` again.

- [ ] **Step 5: Run Astro build with the known good Node runtime**

```powershell
& 'C:\Users\刘\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe' .\node_modules\astro\bin\astro.mjs build
```

Expected: PASS.

- [ ] **Step 6: Re-run RSS verification after build**

```powershell
npm run verify:rss
```

Expected: PASS.

- [ ] **Step 7: Inspect build output markers**

Run:

```powershell
Select-String -Path dist\articles\stable-internet-access\index.html -Pattern "blockquote|table|prose|/images/articles/stable-internet-access"
```

Expected: output includes the article body structure and local image references.

- [ ] **Step 8: Check git status**

```powershell
git status -sb --untracked-files=all
```

Expected: this feature's files are either committed or clearly visible as intentional changes. Existing unrelated local files may still appear and must not be reverted.

---

### Task 7: Final Feature Commit If Needed

**Files:**
- Only files changed after Task 5 or Task 6.

- [ ] **Step 1: Review changed files**

```powershell
git diff --stat
git diff -- package.json scripts/verify-content.mjs scripts/verify-ux.mjs src/styles/global.css docs/内容规范.md docs/templates/post-template.md
```

Expected: only content-standard feature changes appear in the diff.

- [ ] **Step 2: Commit remaining feature changes**

If there are remaining intentional changes from this feature:

```powershell
git add package.json scripts/verify-content.mjs scripts/verify-ux.mjs src/styles/global.css docs/内容规范.md docs/templates/post-template.md
git commit -m "feat: add blog content standards"
```

If all feature changes were already committed by previous tasks, skip this step.

- [ ] **Step 3: Summarize without publishing**

Report:

```text
- What changed
- Which verification commands passed
- Whether Astro build passed
- Which unrelated local files remain untouched
```

Do not push or publish unless Waltz explicitly asks to上线.

---

## Plan Self-Review

- Spec coverage:
  - `docs/内容规范.md`: Task 3.
  - `docs/templates/post-template.md`: Task 3.
  - `scripts/verify-content.mjs`: Task 1 and Task 2.
  - `npm run verify:content`: Task 1, Task 2, Task 5, Task 6.
  - `.prose` style expansion: Task 4 and Task 5.
  - Full verification and build: Task 6.
- Placeholder scan:
  - The plan contains no TBD or placeholder implementation steps.
- Type and name consistency:
  - Script name is consistently `verify-content.mjs`.
  - npm script is consistently `verify:content`.
  - Docs paths are consistently `docs/内容规范.md` and `docs/templates/post-template.md`.

