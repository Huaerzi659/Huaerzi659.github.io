<header class="site-header">
  <div class="container header-inner">
    <a class="brand" href="/" aria-label="回到首页">
      <span class="brand-mark">W</span>
      <span class="brand-text">Waltz<span>/think</span></span>
    </a>
    <nav class="nav" aria-label="主导航">
      <a class="nav-home" href="/">首页</a>
      <a href="/articles/">文章</a>
      <a href="/topics/">主题</a>
      <a href="/about/">关于</a>
      <a href="https://github.com/Huaerzi659" target="_blank" rel="noreferrer">GitHub</a>
    </nav>
  </div>
</header>

<style>
  .site-header {
    position: sticky;
    top: 0;
    z-index: 10;
    border-bottom: 1px solid rgba(0, 245, 255, 0.24);
    background: rgba(5, 6, 15, 0.86);
    backdrop-filter: blur(14px);
  }

  :global(:root[data-theme="anime-scroll"]) .site-header {
    border-bottom-color: rgba(26, 45, 76, 0.24);
    background: rgba(255, 250, 240, 0.88);
  }

  .header-inner {
    min-height: 62px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 18px;
  }

  .brand {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    color: var(--ink);
    font-family: var(--font-mono);
    font-size: 0.95rem;
    font-weight: 700;
    text-decoration: none;
  }

  .brand-mark {
    display: inline-grid;
    width: 30px;
    height: 30px;
    place-items: center;
    border: 1px solid var(--neon-cyan);
    border-radius: var(--radius);
    background: rgba(0, 245, 255, 0.12);
    box-shadow: var(--cyan-glow);
    color: var(--neon-cyan);
    line-height: 1;
    text-shadow: var(--cyan-glow);
    transition: border-color var(--motion-short) var(--ease-out), box-shadow var(--motion-short) var(--ease-out), color var(--motion-short) var(--ease-out), transform var(--motion-short) var(--ease-out);
  }

  .brand:hover .brand-mark,
  .brand:focus-visible .brand-mark {
    border-color: var(--neon-pink);
    box-shadow: var(--pink-glow);
    color: #ffd8f7;
    transform: translateY(-1px);
  }

  :global(:root[data-theme="anime-scroll"]) .brand-mark {
    border-color: rgba(249, 115, 22, 0.62);
    background: var(--stamp-bg);
    box-shadow: 0 8px 18px rgba(249, 115, 22, 0.16);
    color: #9a3412;
    text-shadow: none;
  }

  :global(:root[data-theme="anime-scroll"]) .brand:hover .brand-mark,
  :global(:root[data-theme="anime-scroll"]) .brand:focus-visible .brand-mark {
    border-color: rgba(15, 139, 179, 0.6);
    box-shadow: 0 10px 22px rgba(15, 139, 179, 0.16);
    color: #0f5f8a;
  }

  .brand-text span {
    color: var(--neon-pink);
    text-shadow: var(--pink-glow);
  }

  :global(:root[data-theme="anime-scroll"]) .brand-text span {
    color: #9a3412;
    text-shadow: none;
  }

  .nav {
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 0.94rem;
  }

  .nav a {
    border: 1px solid transparent;
    border-radius: var(--radius);
    padding: 7px 10px;
    color: var(--muted);
    text-decoration: none;
    transition: background-color var(--motion-short) var(--ease-out), border-color var(--motion-short) var(--ease-out), color var(--motion-short) var(--ease-out), text-shadow var(--motion-short) var(--ease-out);
  }

  .nav a:hover {
    border-color: rgba(0, 245, 255, 0.38);
    background: rgba(0, 245, 255, 0.08);
    color: var(--neon-cyan);
    text-shadow: var(--cyan-glow);
  }

  :global(:root[data-theme="anime-scroll"]) .nav a:hover {
    border-color: rgba(249, 115, 22, 0.38);
    background: rgba(249, 115, 22, 0.1);
    color: #9a3412;
    text-shadow: none;
  }

  @media (max-width: 560px) {
    .header-inner {
      align-items: stretch;
      flex-direction: column;
      padding: 12px 0;
    }

    .nav {
      display: grid;
      grid-template-columns: repeat(5, minmax(0, 1fr));
    }

    .nav a {
      text-align: center;
    }
  }
</style>
