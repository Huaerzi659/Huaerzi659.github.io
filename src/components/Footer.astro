<footer class="site-footer">
  <div class="container footer-panel">
    <div>
      <p class="terminal-label">workspace / footer</p>
      <p class="footer-title">Waltz, Then Think</p>
      <p class="footer-copy">Built with Astro. Updated by writing.</p>
    </div>
    <div class="footer-links" aria-label="联系链接">
      <a href="https://github.com/Huaerzi659" target="_blank" rel="noreferrer">GitHub</a>
      <a href="mailto:718231052@qq.com">718231052@qq.com</a>
    </div>
  </div>
</footer>

<style>
  .site-footer {
    padding: 34px 0 42px;
  }

  .footer-panel {
    position: relative;
    overflow: hidden;
    border: 1px solid var(--line);
    border-radius: var(--radius);
    background:
      linear-gradient(135deg, rgba(255, 43, 214, 0.08), transparent 42%),
      rgba(11, 16, 32, 0.84);
    box-shadow: var(--shadow);
    padding: 24px;
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    gap: 20px;
  }

  .footer-panel::before {
    content: "";
    position: absolute;
    top: 0;
    right: 18px;
    left: 18px;
    height: 1px;
    background: linear-gradient(90deg, transparent, rgba(0, 245, 255, 0.7), rgba(255, 43, 214, 0.45), transparent);
    box-shadow: 0 0 18px rgba(0, 245, 255, 0.18);
  }

  .footer-panel > * {
    position: relative;
    z-index: 1;
  }

  .footer-title {
    margin: 0;
    font-family: var(--font-display);
    font-size: 1.35rem;
    font-weight: 720;
  }

  .footer-copy {
    margin: 8px 0 0;
    color: var(--muted);
  }

  .footer-links {
    display: flex;
    align-items: flex-end;
    justify-content: flex-end;
    flex-wrap: wrap;
    gap: 10px;
  }

  .footer-links a {
    border: 1px solid var(--line);
    border-radius: var(--radius);
    background: rgba(0, 245, 255, 0.08);
    padding: 8px 10px;
    color: var(--muted);
    text-decoration: none;
    transition: border-color var(--motion-short) var(--ease-out), box-shadow var(--motion-short) var(--ease-out), color var(--motion-short) var(--ease-out), background-color var(--motion-short) var(--ease-out);
  }

  .footer-links a:hover,
  .footer-links a:focus-visible {
    border-color: var(--neon-pink);
    background: rgba(255, 43, 214, 0.1);
    color: #ffd8f7;
    box-shadow: var(--pink-glow);
  }

  @media (max-width: 680px) {
    .footer-panel {
      grid-template-columns: 1fr;
    }

    .footer-links {
      justify-content: flex-start;
    }
  }
</style>
