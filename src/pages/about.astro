---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout title="关于" description="关于 Waltz 和 Waltz, Then Think。">
  <section class="section about-hero">
    <div class="container workbench-grid">
      <div class="about-copy">
        <p class="terminal-label">about / workspace owner</p>
        <h1 class="section-title">关于这个地方</h1>
        <p>
          这里不是简历，也不是只放标准答案的技术文档。
          我更想把它当成一个长期使用的个人工作台：记录技术、经验、感想，
          也记录那些还没有长成完整文章的灵感。
        </p>
        <p>
          `Waltz, Then Think` 这个名字，对我来说像一个顺序：
          先动起来，先试一小步，然后再停下来想清楚。
        </p>
      </div>

      <aside class="status-card surface">
        <p class="terminal-label">current stack</p>
        <dl>
          <div>
            <dt>writing</dt>
            <dd>技术 / 经验 / 感想</dd>
          </div>
          <div>
            <dt>building</dt>
            <dd>Astro + Markdown</dd>
          </div>
          <div>
            <dt>contact</dt>
            <dd><a href="mailto:718231052@qq.com">718231052@qq.com</a></dd>
          </div>
        </dl>
        <a class="button" href="https://github.com/Huaerzi659" target="_blank" rel="noreferrer">去 GitHub</a>
      </aside>
    </div>
  </section>

  <section class="section contact-section">
    <div class="container contact-panel">
      <p class="terminal-label">why write</p>
      <h2>把想法留下来，才有机会继续迭代。</h2>
      <p>
        有些东西不写下来，就会一直停在“我好像懂了”的状态。
        这个博客会慢慢收纳那些被调试过的想法。
      </p>
    </div>
  </section>
</BaseLayout>

<style>
  .about-hero {
    border-bottom: 1px solid var(--line);
  }

  .about-copy {
    grid-column: span 7;
  }

  .about-copy p:not(.terminal-label) {
    max-width: 720px;
    color: var(--muted);
    font-size: 1.08rem;
    line-height: 1.78;
  }

  .status-card {
    grid-column: 8 / span 5;
    align-self: end;
    padding: 22px;
  }

  dl {
    margin: 0 0 20px;
    display: grid;
    gap: 12px;
  }

  dl div {
    border-top: 1px solid var(--line);
    padding-top: 12px;
  }

  dt {
    color: var(--faint);
    font-family: var(--font-mono);
    font-size: 0.78rem;
  }

  dd {
    margin: 4px 0 0;
    color: var(--ink);
  }

  .contact-panel {
    border: 1px solid var(--line);
    border-radius: var(--radius);
    background:
      linear-gradient(135deg, rgba(255, 43, 214, 0.16), transparent 42%),
      #020617;
    box-shadow: var(--shadow);
    padding: 28px;
    color: var(--white);
  }

  :global(:root[data-theme="anime-scroll"]) .contact-panel {
    border-color: rgba(26, 45, 76, 0.28);
    background:
      linear-gradient(135deg, rgba(249, 115, 22, 0.1), transparent 42%),
      linear-gradient(180deg, rgba(255, 250, 240, 0.96), rgba(243, 230, 208, 0.74));
    box-shadow: 5px 5px 0 rgba(26, 45, 76, 0.1), 0 16px 34px rgba(57, 42, 22, 0.1);
    color: var(--ink);
  }

  :global(:root[data-theme="minimal"]) .contact-panel {
    border-color: var(--line-muted);
    background:
      linear-gradient(135deg, var(--glass-blue), transparent 42%),
      linear-gradient(315deg, var(--glass-coral), var(--glass-lilac) 58%, transparent),
      var(--surface-quiet);
    box-shadow: var(--shadow);
    color: var(--ink);
  }

  .contact-panel .terminal-label {
    color: var(--neon-pink);
    text-shadow: var(--pink-glow);
  }

  :global(:root[data-theme="anime-scroll"]) .contact-panel .terminal-label {
    color: #9a3412;
    text-shadow: none;
  }

  :global(:root[data-theme="minimal"]) .contact-panel .terminal-label {
    color: #be315d;
    text-shadow: none;
  }

  .contact-panel h2 {
    max-width: 760px;
    margin: 0;
    font-family: var(--font-display);
    font-size: clamp(1.7rem, 4vw, 3rem);
    line-height: 1.08;
  }

  :global(:root[data-theme="anime-scroll"]) .contact-panel h2 {
    color: var(--ink);
    text-shadow: none;
  }

  :global(:root[data-theme="minimal"]) .contact-panel h2 {
    color: var(--ink);
    text-shadow: none;
  }

  .contact-panel p:last-child {
    max-width: 680px;
    margin-bottom: 0;
    color: rgba(255, 255, 255, 0.72);
  }

  :global(:root[data-theme="anime-scroll"]) .contact-panel p:last-child {
    color: var(--copy);
  }

  :global(:root[data-theme="minimal"]) .contact-panel p:last-child {
    color: var(--copy);
  }

  @media (max-width: 760px) {
    .about-copy,
    .status-card {
      grid-column: auto;
    }
  }
</style>
