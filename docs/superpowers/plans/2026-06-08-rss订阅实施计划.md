# RSS 订阅 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 `Waltz, Then Think` 添加标准 RSS feed，并让阅读器可以自动发现 `/rss.xml`。

**Architecture:** 使用 Astro 官方 `@astrojs/rss` 在 `src/pages/rss.xml.js` 生成 XML endpoint。RSS 数据来自现有 `posts` 内容集合，排序复用 `src/lib/posts.ts` 中的 `sortPostsByDate()`。构建后用脚本检查 `dist/rss.xml` 的结构和内容。

**Tech Stack:** Astro 6、Markdown content collections、`@astrojs/rss`、Node.js 验证脚本。

---

### Task 1: RSS 输出验证脚本

**Files:**
- Create: `scripts/verify-rss.mjs`
- Modify: `package.json`

- [ ] **Step 1: Write the failing test**

Create `scripts/verify-rss.mjs`:

```js
import { readFile, readdir } from "node:fs/promises";
import { existsSync } from "node:fs";
import path from "node:path";

const root = process.cwd();
const rssPath = path.join(root, "dist", "rss.xml");
const postsDir = path.join(root, "src", "content", "posts");

function assert(condition, message) {
  if (!condition) {
    throw new Error(message);
  }
}

if (!existsSync(rssPath)) {
  throw new Error("dist/rss.xml does not exist. Run npm run build first.");
}

const rss = await readFile(rssPath, "utf8");
const postFiles = (await readdir(postsDir)).filter((file) => file.endsWith(".md"));

assert(rss.includes("<title>Waltz, Then Think</title>"), "RSS channel title is missing.");
assert(rss.includes("Waltz 的个人博客"), "RSS channel description is missing.");
assert(rss.includes("https://huaerzi659.github.io/articles/debugging-life/"), "Latest article link is missing.");
assert(rss.includes("把生活也当成一次调试"), "Latest article title is missing.");

const itemCount = (rss.match(/<item>/g) ?? []).length;
assert(itemCount === postFiles.length, `Expected ${postFiles.length} RSS items, got ${itemCount}.`);

console.log(`RSS verification passed: ${itemCount} items.`);
```

Add script to `package.json`:

```json
"verify:rss": "node scripts/verify-rss.mjs"
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm run verify:rss`

Expected: FAIL with `dist/rss.xml does not exist`.

### Task 2: RSS endpoint

**Files:**
- Modify: `package.json`
- Create: `src/pages/rss.xml.js`

- [ ] **Step 1: Install official RSS package**

Run: `npm install @astrojs/rss`

Expected: `@astrojs/rss` appears in `dependencies`.

- [ ] **Step 2: Implement the endpoint**

Create `src/pages/rss.xml.js`:

```js
import rss from "@astrojs/rss";
import { getCollection } from "astro:content";
import { sortPostsByDate } from "../lib/posts";

export async function GET(context) {
  const posts = sortPostsByDate(await getCollection("posts"));

  return rss({
    title: "Waltz, Then Think",
    description: "Waltz 的个人博客：技术、经验、感想与灵感。",
    site: context.site,
    customData: "<language>zh-CN</language>",
    items: posts.map((post) => ({
      title: post.data.title,
      description: post.data.description,
      pubDate: post.data.date,
      link: `/articles/${post.id}/`,
    })),
  });
}
```

- [ ] **Step 3: Build and verify RSS**

Run: `npm run build`

Expected: build succeeds and emits `dist/rss.xml`.

Run: `npm run verify:rss`

Expected: `RSS verification passed: 3 items.`

### Task 3: RSS auto-discovery and docs

**Files:**
- Modify: `src/layouts/BaseLayout.astro`
- Modify: `README.md`

- [ ] **Step 1: Add RSS discovery link**

In `src/layouts/BaseLayout.astro`, add inside `<head>`:

```astro
<link
  rel="alternate"
  type="application/rss+xml"
  title="Waltz, Then Think RSS"
  href={new URL("rss.xml", Astro.site)}
/>
```

- [ ] **Step 2: Document feed URL**

Add to `README.md`:

```md
## RSS

订阅地址：`https://Huaerzi659.github.io/rss.xml`
```

- [ ] **Step 3: Final verification**

Run: `npm run build`

Expected: build succeeds.

Run: `npm run verify:rss`

Expected: RSS verification passes and article item count matches the Markdown post count.
